{
  "name": "mapbus",
  "version": "1.0.0",
  "description": "MapaBus on Cloudflare Workers",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "keywords": ["cloudflare", "workers", "bus", "transit"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
