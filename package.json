{
  "name": "bus-tracker",
  "version": "1.0.0",
  "description": "Real-time bus tracking system",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "node build.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "googleapis": "^128.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
